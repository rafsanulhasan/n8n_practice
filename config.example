# n8n Configuration Example
# Copy this file to create your own configuration

## n8n Server Configuration

# n8n Server URL (where n8n is running)
N8N_BASE_URL=http://localhost:5678

# Webhook base path
N8N_WEBHOOK_BASE=/webhook

## Available Workflow Endpoints

# Simple Webhook Example
SIMPLE_WEBHOOK_URL=http://localhost:5678/webhook/simple-webhook

# User Registration Workflow
USER_REGISTRATION_URL=http://localhost:5678/webhook/user-registration

# Data Processing Workflow
DATA_PROCESSING_URL=http://localhost:5678/webhook/process-data

## Blazor Configuration

# Development server port
BLAZOR_PORT=5000

# Production URL (when deployed)
# BLAZOR_URL=https://app.yourdomain.com

## Docker Configuration (if using Docker for n8n)

# Docker container name
DOCKER_CONTAINER_NAME=n8n

# Host port mapping
DOCKER_HOST_PORT=5678

# Container port
DOCKER_CONTAINER_PORT=5678

# Volume mount for n8n data
DOCKER_VOLUME_PATH=~/.n8n

## Production Configuration

# Use HTTPS in production
# N8N_BASE_URL=https://n8n.yourdomain.com

# Enable authentication
# N8N_BASIC_AUTH_ACTIVE=true
# N8N_BASIC_AUTH_USER=admin
# N8N_BASIC_AUTH_PASSWORD=your-secure-password

# Database (for production n8n)
# DB_TYPE=postgresdb
# DB_POSTGRESDB_HOST=localhost
# DB_POSTGRESDB_PORT=5432
# DB_POSTGRESDB_DATABASE=n8n
# DB_POSTGRESDB_USER=n8n_user
# DB_POSTGRESDB_PASSWORD=your-db-password

## CORS Configuration (if needed)

# Allowed origins for CORS
# N8N_CORS_ORIGINS=https://app.yourdomain.com,https://www.yourdomain.com

## Logging

# Log level (error, warn, info, verbose, debug)
N8N_LOG_LEVEL=info

# Log output (console, file)
N8N_LOG_OUTPUT=console

## Notes

# - This file is for documentation purposes
# - Blazor WebAssembly doesn't use .env files directly
# - Update URLs in Blazor code: Pages/N8nWebhooks.razor
# - For n8n, set environment variables when starting:
#   docker run -e N8N_BASIC_AUTH_ACTIVE=true n8nio/n8n
